import Modal from "../Modal/Modal"

const AddExperienceModal = ({open, close}) => {

    return (
        <Modal close={close} open={open}>
            Add Experience Modal
        </Modal>
    )
}

export default AddExperienceModal