import tw from "twin.macro";
import styled from "styled-components";

const CategoryStyles = styled.div`
    ${tw`text-purple-900 rounded-xl bg-purple-200 p-2 whitespace-nowrap`}
`;

export default CategoryStyles