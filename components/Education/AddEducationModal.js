import Modal from "../Modal/Modal";

const AddEducationModal = ({open, close}) => {
    return (
        <Modal close={close} open={open}>
            Add Education Modal
        </Modal>
    )
}

export default AddEducationModal